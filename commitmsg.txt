refactor(ms/server): fluxo estável, TwiML garantido e logs de saída

- Boas-vindas só em step==0 (não reseta com 'oi/ola' no meio)
- /bot com try/except + fallback quando reply vazio
- Content-Type XML com charset utf-8
- UID por WaId; persistência com fallback db.json
- /health adicionado; logs com preview da resposta
