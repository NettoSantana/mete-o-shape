feat(server): aceitar GET em /bot (health-check) e manter POST/TwiML

- /bot agora responde GET (200 OK texto) e POST (Twilio WhatsApp)
- Adicionado /admin/ping para health-check explícito
- Estrutura simples de fluxo (ping/menu/eco) em build_reply()
- Preferência por waitress em produção; fallback Flask dev
